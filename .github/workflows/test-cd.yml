name: "CD - Test Manual Approval"

on:
  push:
    branches: [main]

jobs:
  plan:
    name: "Plan (auto)"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Simulando terraform plan..."

  apply:
    name: "Apply (manual approval gate)"
    runs-on: ubuntu-latest
    needs: plan
    environment:
      name: production
    steps:
      - uses: actions/checkout@v4
      - run: echo "Simulando terraform apply..."
